use std::fs::File;
use std::io::Write;
use std::net::Shutdown::Read;
use std::path::PathBuf;
use array_lib::DimSize::{PHS1, READ};
use crate::copy_planner::CopyPlanner;
use crate::object::{ObjectManagerConf, RawLayout};
use crate::scanner::{HostProperties, Scanner};

#[test]
fn config() {



}
